tempo:
  tempo:
    metricsGenerator:
      # -- If true, enables Tempo's metrics generator (https://grafana.com/docs/tempo/next/metrics-generator/)
      enabled: true
      remoteWriteUrl: "https://metrics-monitoring.lab.suxessit.k8s.cloud.uibk.ac.at/api/v1/push"
  tempoQuery:
    # -- if False the tempo-query container is not deployed
    enabled: true
  serviceMonitor:
    enabled: true
